services:
  web:
    image: python:3.12-alpine
    working_dir: /app
    command: python -m http.server 8080 --bind 0.0.0.0
    volumes:
      - ./:/app:ro
    ports:
      - "${APP_PORT:-8080}:8080"
